# Railway Configuration for HRMS Backend
# This tells Railway to build the backend from hrms_backend folder

[build]
builder = "DOCKERFILE"
dockerfilePath = "hrms_backend/Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
